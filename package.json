{
  "name": "spot-auth",
  "version": "1.0.0",
  "description": "backend auth process for ngspotify-app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.88.0"
  }
}
